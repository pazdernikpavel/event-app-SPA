import { Route } from '@angular/router';

import { EventRegisteredComponent } from './event-registered.component';

export const eventRegisteredRoutes: Route[] = [
  {
    path: '',
    component: EventRegisteredComponent
  }
];
